# Daily Loop — 프로젝트 안내

루틴 관리 앱 "Daily Loop". 1인 개발, **바이브 코딩으로 배우면서 만드는 학습 프로젝트** (학원 병행, 하루 약 2시간 작업 가능).

## 현재 진행 상황 (2026-08-31 기준 — 새 세션 시작 시 여기부터 확인)

**다음 세션 시작 지점**: 2026-08-29~31 세션에서 데이터 레이어를 `react-query`로 전면 통일하는 큰 리팩터링을 마쳤고(오늘/캘린더/통계 탭부터 즐겨찾기·모음집·내 루틴·루틴 복구·영상·설정·일기·루틴 폼까지 전 화면), 그 위에서 폰 QA를 여러 라운드 돌며 트래킹 기록 UI, 슬롯 체크형/정확한 시간 모드, 모음집 중복 생성 버그, 타임라인 표시 등을 계속 고쳤다. **마이그레이션 `0023_add_slot_instant_type.sql`, `0024_update_default_slot_times.sql`를 Supabase에서 실행했는지 다음 세션 시작하면 바로 확인할 것** — 안 하면 슬롯 관련 화면에서 에러 남. `0017`~`0022`는 이번 세션에 그 기능들(시각 체크 타입 등)을 실제로 계속 사용하며 테스트해서 이미 실행돼 있는 것으로 확인됨. 코드는 전부 `npx tsc --noEmit` 통과 상태.

**사용자가 직접 정리해야 할 것**: 저번 세션에 모음집 "적용" 중복 버그로 4개까지 불어났던 모음집이 있으면, "내 루틴 → 모음집" 필터에서 겹치는 2개를 직접 골라 삭제해야 함(자동 정리 안 됨, 원인은 이번 세션에 고침 — 앞으로는 안 불어남).

**구글 로그인 테스트 방법이 바뀜(2026-08-31)**: 기존 `--tunnel`(ngrok) 방식이 번들된 구버전 때문에 계속 막혀서, **USB + `adb reverse`** 방식으로 전환함. `adb`는 winget으로 이미 설치해둠(`Google.PlatformTools`). 절차: 폰 USB 연결(디버깅 허용) → `npx expo start`(터널 없이) → `adb reverse tcp:포트 tcp:포트` → Expo Go에서 "Enter URL manually"로 `exp://localhost:포트` 접속. 평소 개발은 이 방식 필요 없고 기존처럼 와이파이 QR 스캔이면 충분 — 로그아웃 후 재로그인 테스트할 때만 필요.

### 📱 2026-08-29~31 세션 요약 (상세는 `docs/study.md` "2026-08-29~31" 섹션 참고)

- **데이터 레이어 표준화(react-query 도입)**: 화면마다 제각각이던 "포커스 시 재조회"/캐싱 로직을 `@tanstack/react-query`로 통일. 마이그레이션 전 `docs/data-layer-migration-checklist.md`에 기존 기능을 전부 적어두고 화면 옮길 때마다 대조 검증. 여러 화면이 같은 쿼리 키(`slots`/`favorites`/`stats`/`presets`/`streak-configs`/`llm-quota`/`video` 등)를 공유해서 한 화면에서 바꾼 게 다른 화면에도 자동 반영됨. 신규 파일 `lib/query-client.ts`(QueryClient 설정+AsyncStorage 오프라인 캐시), `lib/use-refetch-on-focus.ts`
- **체크박스/트래킹 저장에 낙관적 업데이트 적용** — 서버 응답 기다리지 않고 화면부터 먼저 바뀌게 해서 "체크할 때 렉 걸린다"는 문제 해결
- **트래킹형 루틴에 "취소"(기록삭제) 기능 신규**: 기록 있으면 "✓ 오늘 기록 N" 표시 + 수정/기록삭제 버튼, 없으면 입력창+저장 버튼으로 화면이 전환됨(저장 성공 여부를 눈으로 바로 확인 가능). 화면 아래쪽 루틴 수정 시 키보드에 가려지던 버그도 수정(포커스 시 위로 스크롤 + 키보드 다 올라온 시점에 한 번 더 강제 보정)
- **슬롯(아침/점심/저녁/자기전)에 체크형/정확한 시간 모드 추가**: 기본은 체크형, 신규 가입자는 아침7/점심12/저녁18/자기전22시로 기본 설정(마이그레이션 `0024`). 모드 전환해도 커스텀한 시간값은 유지됨(전환 시 시간을 자동으로 리셋했다가, 피드백 받고 값은 안 건드리고 모드 플래그만 바꾸는 걸로 단순화). 신규 마이그레이션 `0023_add_slot_instant_type.sql`(`slots.is_instant`)
- **모음집 "오늘 목록에 적용" 중복 생성 버그 수정**: 이미 그 모음집으로 만든 살아있는 루틴이 있으면(일시정지 포함) 새로 안 만들고 일시정지만 풀어서 재사용 — 예전엔 "전체 비활성화" 후 재적용 시 2개→4개로 계속 불어났음(`lib/presets.ts`의 `applyPreset`)
- **오늘 탭에서 스와이프로 "오늘 삭제"한 루틴을 되돌리는 기능 신규**: "내 루틴" 화면에 "오늘 제외됨" 표시 + "오늘 목록에 추가" 버튼(`lib/routines.ts`의 `fetchSkippedRoutineIds`/`unskipRoutine`)
- **타임라인에서 슬롯이 "정확한 시간"이면 그 시간대 길이만큼 실제 높이로 블록 표시**(체크형이면 기존처럼 작은 점 블록), 슬롯 선택 칩에 실제 시각도 같이 표시(`slotTimeLabel`), 오늘 탭 리스트에도 슬롯 시각 보조 표시 추가
- **크래시 버그 2건**: (1) `today-skips` 쿼리가 `Set` 타입을 담는데 오프라인 캐시(AsyncStorage)에 저장됐다 복원되면서 깨져서 "내 루틴" 탭이 안 열리던 문제 — 캘린더 탭 때와 같은 원인(Map/Set은 JSON 직렬화하면 망가짐), `lib/query-client.ts`의 저장 제외 목록에 추가 + 캐시 저장 키 버전 올려서 기존 깨진 캐시 폐기
- 설정 화면 슬롯 시계를 다른 화면과 같은 스피너 방식(15분 단위)으로 통일, 체크형/정확한 시간 토글을 칩 2개 대신 버튼 1개로 병합해 시간 표시 줄과 한 줄로 압축, 설명 문구(체크형/정확한 시간 안내, 메모 알림 안내)는 최초 1회만 자동으로 보이고 그다음부턴 ⓘ 아이콘으로 접힘
- 즐겨찾기/설정 화면 시계를 루틴 폼과 같은 스피너(iOS: 계속 열린 스피너+완료 버튼, 안드로이드: 네이티브 다이얼로그) 방식으로 통일

### 📱 2026-08-27~28 세션 요약 (상세는 `docs/study.md` "2026-08-27-2번째" 섹션 참고)

- **루틴 시간 타입에 "시각 체크" 추가**: "8시 기상"처럼 시간을 안 차지하고 그 순간에 체크만 하는 타입. 루틴 폼/모음집 항목/즐겨찾기 셋 다 적용. 시간 선택기는 15분 단위로, 끝 시각이 시작보다 빠르면 막음(자정 00:00만 예외). 신규 마이그레이션 `0021_add_instant_time_type.sql`(`routines`/`preset_items`/`routine_favorites`에 `is_instant` 컬럼)
- **⚠️ 삭제 정책 대전환 — 루틴은 이제 절대 완전삭제(하드 삭제) 안 함**: 기존엔 "루틴 복구" 화면에서 "선택 삭제"로 2주 안 기다리고 즉시 완전삭제가 가능했는데, 이게 완료기록까지 CASCADE로 같이 지워버리는 구조였음(실제로 사용자가 테스트하다 약 2달치 기록을 이 기능으로 영구 삭제한 사고 있었음, 복구 불가). "루틴을 그만 쓰는 것"과 "기록을 지우는 것"은 다른 결정이라 판단해 루틴은 소프트 삭제만 하고 절대 하드 삭제 안 하는 걸로 구조를 바꿈. 대신 "루틴 복구" 목록만 정리하고 싶을 때 쓰는 `archived_at` 컬럼 신설(신규 마이그레이션 `0022_add_routine_archive.sql`) — 목록에서만 안 보이고 기록은 그대로 보존. 모음집(템플릿)은 완료기록이 안 달려있어 완전삭제 그대로 유지. `lib/routines.ts`에서 `hardDeleteRoutines`/`hardDeleteRoutinesByPreset`/`purgeOldDeletedRoutines`(2주 자동 완전삭제) 전부 제거
- **삭제해도 과거 기록 유지 + 복구 시 스트릭 보존**: 캘린더/통계가 삭제된 루틴도 삭제 전 날짜까지는 계속 포함하도록 수정(`matchesToday`에 생성일/삭제일 기준 판정 추가 — 이 로직을 뺐다가 다시 넣는 시행착오가 있었음, 자세한 경위는 study.md 참고). 복구할 때는 삭제돼 있던 기간을 자동으로 "건너뛴 날짜" 처리해서 스트릭이 억울하게 안 끊기게 함(`backfillSkipDatesForGap`)
- **역대 최고 스트릭(`bestStreakEver`) 개념 신설**: 지금까지 있었던 모든 루틴(삭제된 것 포함) 통틀어 가장 길었던 스트릭 하나만 계산해서 보존 — 루틴을 지워도 이 기록만은 안 사라짐. 캘린더 주간뷰 위에 배지로 노출
- **날짜 계산 타임존 버그 수정**: `created_at`/`deleted_at`이 UTC로 저장되는데 앞 10글자만 잘라 비교하고 있어서, 한국 새벽(자정~오전9시) 근처에 만들거나 지운 루틴은 하루 어긋나 보이던 버그 수정(`localDateOf` 헬퍼로 로컬 날짜 기준 재계산)
- **캘린더 화면 전면 개편**: 기본 화면 월간뷰→주간뷰, 다른 탭 갔다 오면 항상 이번 주 주간뷰로 리셋, 오늘 날짜가 화면에 바로 보이도록 가로 스크롤 자동 조정, "월" 누르면 항상 실제 이번 달로 리셋, **로딩 스피너 완전 제거**(날짜 칸은 즉시 그려지고 색/일정은 도착하는 대로 채워짐), 이미 본 달/주는 재요청은 하되 화면은 안 건드리다 조용히 최신화(Stale-While-Revalidate), `CalendarList` 과거/미래 준비 범위 50개월→24/12개월로 축소(초기 로딩 부담 감소)
- 통계 화면 안내 문구 최초 1회만 노출로 변경, 삭제된 루틴 정보를 빈 목록 안내에 통합
- 테스트용 SQL 스크립트 준비해둠(가상 루틴 5개, 각각 다른 완료 패턴 — 필요하면 대화 기록 참고해서 재사용)

- 캘린더 아무 날짜(과거·오늘·미래 전부)나 눌러서 짧은 메모를 여러 개 남길 수 있음. 하루당 하나뿐인 기존 "일기"와 별개 기능
- 색상 5개(노랑/빨강/민트/파랑/보라) 중 골라서 포스트잇처럼 표시 — 색의 의미는 사용자 자유
- 월간 캘린더 칸: 날짜 숫자 아래에 그날 메모 색상만큼 작은 파스텔 반투명 바가 위아래로 쌓여서 보임(최대 5개까지 칸에 표시, 실제 저장 개수는 제한 없음). 주간뷰는 칸 상단에 작은 점으로 동일하게 표시
- 날짜 눌러서 여는 바텀시트: 메모 목록(수정/삭제 가능) + 새 메모 추가폼이 위쪽, 그날 루틴 목록이 아래쪽 — 한 번에 스크롤
- 신규 파일: `supabase/migrations/0017_add_date_memos.sql`(테이블+RLS), `lib/date-memos.ts`(CRUD). 수정 파일: `app/(tabs)/calendar.tsx`(월간뷰를 `dayComponent` 커스텀 렌더링으로 교체, 주간뷰·모달에 메모 UI 추가)
- 이어서 메모 추가 버튼이 한 번에 안 눌리던 버그 수정(모달 안 `ScrollView`에 `keyboardShouldPersistTaps="handled"` 추가 — 키보드 뜬 상태에서 첫 탭이 키보드 닫기로만 소모되던 RN 기본 동작 때문)
- **아침 알림 + 메모 알림, 독립 채널(2026-08-26)**: "아침 루틴 알림"과 "메모 알림"은 각자 토글로 따로 켜고 끔(`slots.notify_enabled` / `slots.memo_notify_enabled`, 둘 다 아침 슬롯 카드에 노출). 아침 시각 기준으로 — 둘 다 켜져 있고 그날 메모가 있으면 알림 하나로 합쳐서 발송, 아침만 켜져 있으면 기본 문구만, 메모만 켜져 있고 그날 메모가 있으면 메모만 발송(아침 알림이 꺼져 있어도 감), 메모 토글이 꺼져 있으면 메모는 절대 안 붙음. **구현상 트레이드오프**: 매일 문구가 그날그날 달라져야 해서, 기존처럼 "매일 반복" 알림이 아니라 자기전 리마인더처럼 "앱 열 때마다 다음 발송 시점 1회성으로 재계산"하는 방식으로 바뀜 — 앱을 며칠 안 열면 그 사이엔 아침/메모 알림이 안 갈 수 있음(자기전 알림도 같은 한계를 이미 갖고 있었음, 매일 앱을 여는 습관 앱 특성상 감수하기로 함). 신규 마이그레이션 `0018_add_memo_notify.sql`, 수정 파일: `lib/routines.ts`(Slot 타입), `lib/notifications.ts`(`scheduleMorningAlarm` 신규), `app/settings.tsx`(토글 UI), `app/(tabs)/calendar.tsx`(메모 추가/삭제 시 알림 재동기화)

- **영상 카테고리 커스텀화(2026-08-26)**: 기존엔 관리자가 시드한 카테고리 6개(운동/뷰티/독서/모닝루틴/마인드풀니스/공부자기계발)만 있고 유저는 읽기만 가능했는데, 이제 유저가 직접 카테고리를 만들고("+ 카테고리" 탭 버튼) 자기 카테고리에 한해 이름 수정("✎ 이름 수정")·삭제("🗑 삭제")가 가능해짐. 삭제는 소프트 삭제 후 **3일간 보관**(루틴 2주/모음집 2주보다 짧게), "삭제된 카테고리" 링크에서 복구하거나 기다리지 않고 즉시 완전 삭제 가능. 완전 삭제되면 그 카테고리의 영상도 같이 지워짐. `app/videos.tsx`의 "내 영상" 탭(`components/CategoryVideoGrid.tsx`)에서만 카테고리 관리 가능 — "추천 영상" 탭은 관리자 콘텐츠라 기본 카테고리만 그대로 보임. 신규 마이그레이션 `0019_add_custom_categories.sql`(categories에 `user_id`/`deleted_at` 추가, id를 identity 자동증가로 전환, RLS를 "기본+내 것만 조회"로 좁힘, 카테고리 삭제 시 영상도 cascade 삭제되도록 FK 변경). 수정 파일: `lib/videos.ts`(카테고리 CRUD+휴지통 함수 추가), `components/CategoryVideoGrid.tsx`, `components/RecommendedVideoGrid.tsx`(기본 카테고리만 쓰도록 분리), `app/(tabs)/index.tsx`(카테고리 3일 자동 정리를 기존 루틴/모음집 정리 루틴에 합류)
- **기본 카테고리도 삭제 가능하게 확장(2026-08-26)**: 기본 카테고리 6개는 모든 유저가 공유하는 행이라 진짜로 삭제하면 다른 유저에게도 영향이 가서, 유저별로 "숨김" 처리하는 별도 방식으로 구현(`hidden_default_categories` 테이블, 신규 마이그레이션 `0020_hide_default_categories.sql`). "삭제된 카테고리" 화면에 숨긴 기본 카테고리 목록 + **"기본 카테고리 생성"** 버튼을 추가해서, 누르면 숨긴 기본 카테고리들이 한 번에 다시 나타남. 일부만 삭제했으면 그 일부만 다시 생김. 삭제/복원 시 영상이 정확히 어떻게 되는지는 최종 정책이 아래 2026-08-27 "기본 카테고리 삭제/복원 정책 확정" 항목에 있음(이 항목 작성 시점 이후 두 번 조정됨). 수정 파일: `lib/videos.ts`(`fetchCategories`가 userId를 받아 숨김 목록 제외, `hideDefaultCategory`/`fetchHiddenDefaultCategories`/`recreateDefaultCategories` 신규), `components/CategoryVideoGrid.tsx`(삭제 버튼이 기본/커스텀 분기 처리, 삭제된 카테고리 모달에 기본 카테고리 섹션 추가)
- **캘린더에 일기 작성 여부 표시(2026-08-26)**: 그동안 어느 날짜에 일기를 썼는지 캘린더에서 알 수 없었던 문제 해결. 월간뷰는 날짜 칸 맨 위(숫자보다 위쪽)에, 주간뷰는 각 요일 칸 맨 위에 작은 📖 아이콘을 표시 — 그 날짜에 일기가 있을 때만 보임. 신규 함수 `lib/diary.ts`의 `fetchDiaryDatesInRange`(범위 안에서 일기 있는 날짜만 조회), 수정 파일: `app/(tabs)/calendar.tsx`(`diaryDates` 상태 추가, 월/주 로딩 시 같이 조회)
- **오늘 탭 타임라인 뷰 초기 스크롤 위치 수정(2026-08-26)**: 첫 일정이 늦은 시각(예: 10시)이어도 타임라인이 항상 6시부터 그려져서 화면 첫 진입 시 위쪽에 빈 공간만 보이던 문제 → 화면을 열면 그날 제일 이른 일정이 바로 보이는 위치로 자동 스크롤되도록 수정(그보다 이른 시각은 위로 스크롤하면 그대로 볼 수 있음, 데이터 범위 자체는 안 바뀜). `app/(tabs)/index.tsx`의 `TimelineView`에 `onContentSizeChange`로 최초 1회만 자동 스크롤하는 로직 추가
- **폰 QA 4건 수정(2026-08-27)**:
  1. 오늘 탭 리스트에서 루틴 제목(예: "물마시기")을 눌러도 ✎ 아이콘처럼 수정 화면으로 들어가게 함(`app/(tabs)/index.tsx`, `titleLine`을 `Pressable`로 변경). 스크롤 동작에는 영향 없음(`Pressable`은 원래 드래그와 탭을 구분함)
  2. 타임라인에서 같은 슬롯(예: 아침)에 루틴이 여러 개 몰리면 한 블록에 위아래로 쌓여서 마치 오전 11시~오후 3시처럼 긴 시간대인 것처럼 보이던 문제 → 쌓지 않고 옆으로 나란히 배치하도록 변경. 5개 컷 → 3개 기준 → 최종적으로 **"겹치면 무조건 1개만 보여주고 나머지는 '더보기' 버튼"**으로 정착(버튼 누르면 위아래로 1개씩 전부 펼쳐짐, 펼친 상태에서 스크롤을 시작하면 자동으로 다시 접힘 — `onScrollBeginDrag`으로 처리). "더보기" 버튼이 처음엔 항목과 거의 같은 크기(48%)였는데 "너무 크다"는 피드백으로 **버튼은 16%, 항목은 80%**로 재조정. `assignColumns`에 `clusterId` 추가, `TimelineView`에 `renderBlock` 헬퍼와 클러스터 단위 렌더링 추가
  3. `components/CategoryVideoGrid.tsx`의 모달 3개(내 영상 추가/카테고리 추가·수정/삭제된 카테고리) 전부 바깥 영역 탭으로 닫히지 않던 문제 → 배경에 `Pressable`(절대영역) 추가해서 바깥 탭 시 닫히도록 수정
  4. **기본 카테고리 삭제/복원 정책 확정**: "기본으로 주는 영상은 복원하면 다시 줘야 한다"는 요청으로 두 번 조정함. 최종 동작 — 기본 카테고리를 삭제(숨김)하면 그 안에 **내가 직접 추가한 영상은 즉시 삭제(복구 불가)**, "기본 카테고리 생성"으로 다시 보이면 **저희가 기본 제공하는 추천 카탈로그 영상만 자동으로 다시 채워짐**(직접 추가한 건 안 돌아옴). `recreateDefaultCategories`가 숨겨졌던 카테고리들의 관리자 기본 영상(`videos.user_id is null`)을 내 그리드에 재삽입(이미 있으면 URL 기준으로 중복 방지). 삭제 전 안내가 약하다는 피드백으로 확인창 제목을 "⚠️ 추가한 영상은 복구되지 않아요"로 바꿔서 삭제 전에 한 번 더 명확히 경고하도록 함
- **오늘 탭 "지금 시간" 강조 개선(2026-08-27)**: 리스트는 기존에도 지금 시각에 해당하는 루틴 행이 강조됐지만 정확한 시각형 루틴만 인식했고(슬롯형은 강조 안 됨), 타임라인엔 표시가 아예 없었음.
  - 신규 `isNowWithinRange` 함수로 정확한 시각형/슬롯형 둘 다 "지금이 그 범위 안인지" 판정하도록 통일 — 리스트도 이걸로 교체해서 슬롯형 루틴도 이제 강조됨. 기존 시각형 전용 `isHappeningNow`(+ 전용 헬퍼 `formatLocalTime`)는 더 안 쓰여서 `lib/routines.ts`에서 삭제
  - 타임라인엔 ①현재 정확한 시각(예: 3시30분이면 정확히 그 위치) 기준 가로 강조선 추가 — "티가 안 난다"는 피드백으로 동그란 점+시각 텍스트 라벨을 더했다가, 결국 "그냥 처음의 주황 반투명 선 느낌만 남기고 나머지는 빼달라"는 요청으로 **점/라벨은 삭제하고 `nowLine`(반투명 주황 띠) 하나만 남김** ②그 시각에 걸리는 루틴 블록은 배경색+제목 밑줄로 별도 강조(`blockNow`/`blockTitleNow`)
- **설정 화면에 알림 트러블슈팅 안내 추가(2026-08-27)**: 아침·메모 알림이 "앱을 열 때마다 다음 발송 시각을 재계산"하는 방식이라 며칠 안 열면 안 올 수 있다는 걸 사용자가 모를 수 있어서, 슬롯 알림 섹션에 "알림이 안 온다면 앱을 한 번 열어주세요" 안내 문구(`sectionNote`) 추가. 이 안내문이 화면 열 때마다 계속 보여서 거슬린다는 피드백으로, **최초 1회만 자동으로 펼쳐 보여주고(`AsyncStorage` 키 `settings_notice_seen`) 그다음부터는 회색 "ⓘ" 아이콘만 남아있다가 누르면 다시 펼쳐지는 방식**으로 변경
- **루틴 추가/수정 폼 — 정확한 시각 입력 개선(2026-08-27)**: `app/routine-form.tsx`. 기존엔 신규 루틴 기본 시작/끝 시각이 09:00~10:00으로 고정이었고, 시작 시각을 늦은 시각(예: 오후 5시)으로 바꿔도 끝 시각이 그대로 남아있어서(예: 오전 10시) 끝이 시작보다 빠른 것처럼 보이는 문제가 있었음.
  - 신규 루틴 기본값을 **지금 시각(분 단위는 다음 정각으로 올림, 예: 5시47분→6시, 6시1분→7시) ~ 그 +1시간**으로 변경(수정 화면·즐겨찾기·LLM 미리보기로 불러온 값은 그대로 각자 값 사용) — `roundUpToHour` 헬퍼
  - 시작 시각을 바꾸면 끝 시각도 같이 밀리게 하는 로직을 두 번 손봄: 처음엔 "기존 시작~끝 간격을 유지"하는 방식으로 했는데, 이게 오히려 "+30분 등으로 끝 시각을 만져둔 뒤 시작을 바꾸면 여전히 그 늘어난 간격이 유지돼서 다시 +1시간으로 안 맞춰지는 것처럼 보이는" 혼란을 만듦 → **시작 시각이 바뀌면 무조건 끝 시각 = 시작 +1시간**으로 단순화(`applyStartTime`)
  - **"+30분 / +1시간 / +2시간"** 버튼 신규 추가 — 처음엔 시작 시각을 조정하게 했다가 "끝나는 시각을 조정하는 용도로 하자"는 요청으로 **끝 시각 전용**으로 변경(`addMinutesToEnd`). 누르면 현재 끝 시각에 그만큼 누적으로 더해짐(같은 버튼 두 번 누르면 두 배). 그 옆에 **"초기화"** 버튼 추가 — 버튼으로 더한 만큼만 정확히 되돌림(시작+1시간 기본값 자체는 안 건드림, `quickAddedMinutes`로 누적치 추적)
  - "시계가 불편하다"는 피드백으로 시간 선택 UI를 `display="spinner"`(휠 스크롤 방식)로 교체하면서 **"완료"/"취소"를 눌러도 시계가 계속 다시 뜨는 버그**가 새로 생김 → 원인은 안드로이드에선 `mode="time"`이 원래 OS 다이얼로그로 뜨고 확인/취소 시 다이얼로그가 스스로 닫히는 구조라, 매번 `showXPicker`를 꺼줘야 하는데 iOS 방식(계속 열려있는 스피너 + "완료" 버튼)으로만 처리해서 안드로이드에서 꺼지지 않고 계속 재오픈된 것 — **`Platform.OS`로 분기**: 안드로이드는 `display="spinner"`(휠 모양 다이얼로그)를 그대로 쓰되 확인/취소 시 자동으로 닫히는 기존 방식(`handleTimeChange`) 유지, iOS만 계속 열려있는 스피너+"완료" 버튼(`handleSpinnerTimeChange`) 방식 사용
  - 시작~끝 시각 옆에 회색 글씨로 소요 시간("1시간", "1시간 30분" 등) 표시 추가(`formatDuration`)
- **자정 걸친 루틴(예: 23:00~24:00)이 타임라인에 안 보이던 버그 수정(2026-08-27)**: 시계로는 24:00을 고를 수 없어서 자정에 끝나는 루틴은 끝 시각이 `00:00`으로 저장되는데, 이걸 그대로 "끝이 시작보다 이른 시각"으로 계산해버려서 소요 시간이 음수가 되고 타임라인 표시 범위 계산도 깨져서 아예 화면 밖으로 밀려나 있었음. `app/(tabs)/index.tsx`에 `endMinutes()` 헬퍼 추가 — 끝이 시작보다 작거나 같으면 24:00(자정)으로 취급해서 타임라인 높이/범위/"지금" 강조/리스트의 "23:00-24:00" 표시 전부 이 기준으로 통일. `app/routine-form.tsx`의 `formatDuration`도 같은 방식으로 맞춤. (진짜 "다음날로 넘어가는" 자정 통과가 아니라 "이 날의 24:00에 끝난다"로 해석하는 것 — 완료기록·통계 등 다른 로직까지 자정 교차를 지원하는 건 아님)
- **폰 QA 3건 추가 수정(2026-08-27)**:
  1. 오전 10시대처럼 **첫 일정보다 이른 시각에는 타임라인의 "지금" 표시선이 안 보이던 버그** — 원인은 화면을 열 때 "첫 일정이 바로 보이게" 자동 스크롤하는 기능(전날 추가)이 지금 시각보다 아래로 스크롤해버려서 표시선이 화면 위로 밀려나 있었던 것. 자동 스크롤 목표를 "첫 일정과 지금 시각 중 더 이른 쪽"으로 수정
  2. **"말로 루틴 추가"로 만들고 뒤로가기로 나갈 때 키보드 때문에 두 번씩 눌러야 하던 문제**: 처음엔 `skipAutoFocus` 파라미터로 땜질했는데, 근본 원인은 따로 있었음 — 저장 없이 뒤로 나갈 때 `router.replace('/llm-input')`를 써서 스택에 이미 있던 입력 화면 인스턴스는 그대로 둔 채 **새 인스턴스를 하나 더 쌓고** 있었음. 그래서 (a) 새 인스턴스의 `autoFocus`가 다시 켜져서 키보드가 또 뜨고 두 번 눌러야 했고, (b) 뒤로가기로 나갔다 다시 들어가면 예전 글이 안 보이는 문제(진짜 인스턴스는 그대로 있는데 스택 아래 묻혀버림)도 같이 있었음. **`router.dismissTo('/llm-input')`로 바꿔서 새로 안 만들고 스택에 있던 그 화면으로 그대로 돌아가게 수정**(기존 `skipAutoFocus` 땜질은 제거) — 두 문제 다 해결됨. `llm-input.tsx`엔 이 화면이 "진짜로" 스택에서 제거될 때만(routine-form을 미리보기로 push할 땐 안 걸림) 초안 텍스트를 지우는 `beforeRemove` 리스너 추가 — 뒤로가기로 완전히 나갔다 나중에 새로 열면 이제 빈 화면으로 시작함
  3. 루틴 폼 "정확한 시각"에 **"-30분" 버튼** 추가("+30분" 왼쪽) — 끝 시각을 30분 앞당김(시작 시각보다 앞으로는 못 감)
  4. **캘린더 월간뷰/주간뷰 좌우 스와이프 — 커스텀 제스처 시도 전부 실패, 결국 라이브러리 기능으로 교체**:
     - 1차: `PanResponder`(레거시 방식) → 아예 반응 없음(day cell의 `Pressable`이 응답자를 먼저 가져감)
     - 2차: `react-native-gesture-handler`의 `Gesture.Pan`/`GestureDetector`로 교체 → 인식은 됐지만 **스와이프하면 앱이 통째로 튕겨 나가는 문제 발생**(안드로이드 제스처 내비게이션의 "화면 양 끝 스와이프 = 시스템 뒤로가기/앱 전환" 영역과 충돌하는 것으로 추정)
     - 3차: `hitSlop`으로 화면 양 끝을 제외해봤지만 **여전히 튕겨 나감** — 커스텀 제스처로 이 문제를 안정적으로 피하기 어렵다고 판단
     - **최종: 커스텀 제스처를 전부 걷어내고, 월간뷰는 `Calendar` 대신 `CalendarList`(`horizontal` + `pagingEnabled`)로 교체** — 라이브러리가 원래 지원하는 네이티브 FlatList 기반 페이징이라 이런 시스템 제스처 충돌 없이 안전하게 스와이프 지원. **주간뷰는 스와이프 기능 자체를 포기하고 화살표(‹ ›) 버튼만 남김**(대응하는 라이브러리 컴포넌트가 없고, 커스텀 제스처가 계속 실패해서 안전한 쪽을 택함) — 스와이프가 필요하면 나중에 다른 방식으로 다시 요청할 것
- **"내 루틴" 모음집 필터 표시 방식 — 열 번째 조정 끝에 최종 확정(2026-08-27)**:
  1차: "3줄 그리드"(3개씩 끊어 열로) → 모음집 3개면 세로 1줄에 다 차서 어색함
  2차: 표준 `flexWrap`(칩이 줄 끝에 안 맞으면 통째로 다음 줄로 밀림, 자리가 남아도 낭비됨)
  3차: 칩을 화면 밖에 숨겨서 실제 폭을 `onLayout`으로 측정(`presetChipWidths`)한 뒤 그 값으로 줄 배치 계산 → **버그 발생: 한 줄에 10개 넘게 다 몰려서 들어가는 등 레이아웃이 완전히 깨짐**(비동기 2단계 측정 방식이라 컨테이너 폭/칩 폭이 안정적으로 잡히기 전에 계산이 먼저 도는 등 타이밍 문제로 추정)
  4차: **비동기 측정을 완전히 버리고 동기 방식으로 전환** — 칩 폭을 실제 렌더링 없이 문자 수 기반으로 추정(`estimateChipWidth`: 한글은 13px, 그 외는 7.5px로 가중치, 좌우 패딩+테두리 30px 더함)하고, 컨테이너 폭도 `onLayout` 대신 `Dimensions.get('window').width - 40`으로 즉시 계산 → **버그 발생: 폭이 좁게 잘리는 칩의 글자가 가로가 아니라 세로로 한 글자씩 쌓여 보이고, 그 여파로 둘째 줄이 첫째 줄을 가리는 문제**
  5차: `position:absolute`로 넣은 칩에 `alignSelf:'flex-start'`를 추가해 억지로 늘어나는 걸 막아보려 했으나 **효과 없음, 증상 그대로** — `alignSelf`로 해결이 안 된 걸로 보아, RN(Yoga)에서 `position:'absolute'`인 자식은 텍스트 줄바꿈을 계산할 때 부모의 좁은 `width`를 기준으로 삼는 경우가 있어(표준 CSS와 달리 Yoga만의 동작), `alignSelf`만으로는 이 "측정 단계"의 폭 제약 자체를 못 없애는 것으로 보임 — 즉 `position:absolute` + `overflow:hidden` 클리핑 기법 자체가 근본적으로 텍스트 줄바꿈에 취약한 구조였던 것
  6차: `position:absolute` 클리핑 기법을 버리고 표준 `numberOfLines={1}` 말줄임(...)으로 교체 → 줄바꿈 버그는 해결됐지만, 사용자가 "..."으로 뭉개기보다 **잘린 칩을 오른쪽으로 당겨서 마저 보고 싶다**고 피드백— 말줄임 대신 "옆에 더 있다"는 힌트가 되는 자연스러운 클리핑 + 실제 스크롤 기능을 요청
  7차: **각 줄을 개별 가로 `ScrollView`로 감싸는 구조로 전환** — `layoutPresetRows`는 여전히 문자 수 추정 폭으로 "이 줄에 몇 개가 들어가는지"만 정하고(줄이 꽉 차면 다음 줄로 이월), 칩 자체는 폭을 강제로 자르지 않고 항상 자연스러운 전체 크기로 렌더링. 그 줄의 총 내용 폭이 화면보다 넓으면 마지막 칩이 화면 밖으로 살짝 걸쳐 보이는데(말줄임 없이 그대로 잘려서 "옆에 더 있다"는 시각적 힌트가 됨), 그 줄 자체가 가로 스크롤 가능해서 오른쪽으로 당기면 마저 볼 수 있음 → **1번째 줄은 잘 되는데 2번째 줄부터는 가로 스크롤이 안 먹는 버그 발생**
  8차: 줄마다 `nestedScrollEnabled`를 추가해봤지만 **여전히 제대로 안 됨** — 중첩 스크롤 자체가 이 구조(세로 스크롤 안에 가로 스크롤 여러 개)에서 안정적으로 동작하지 않는 것으로 판단
  9차: 폭 추정/가로 스크롤을 전부 버리고 한 줄에 무조건 4개씩 고정 배치, 칸보다 긴 이름은 `numberOfLines`+`ellipsizeMode="clip"`으로 잘림 → 동작은 됐지만, 이름이 8글자 넘으면 그 칩만 어차피 다시 볼 방법이 없어서 사용자가 "이름을 다 보여주고 8글자부터만 자르되, 대신 전체를 가로로 스크롤해서 볼 수 있게 하자"로 요청 변경
  10차(최종): **항상 정확히 3줄만 유지**하도록 `layoutPresetRows` 재작성 — 처음 12개(4개씩)는 순서대로 채우고, 그 이후로 추가되는 모음집은 1번째→2번째→3번째 줄에 하나씩 돌아가며 배정(줄 사이 개수가 항상 균형 잡힘). 칩 이름은 `truncatePresetName`으로 8글자까지만 자르고(픽셀 폭 계산 없이 그냥 문자 수로 자름), 3줄 전체를 **하나의 가로 `ScrollView`로 묶어서** 화면보다 넓어지면 오른쪽으로 당겨서 봄 — 이전처럼 줄마다 개별 가로 스크롤을 쓰지 않고 3줄을 감싸는 `<View>` 하나만 가로 스크롤 안에 넣어서, 중첩 스크롤(세로 안에 가로 여러 개) 자체가 구조적으로 발생하지 않음. 세로 스크롤은 더 이상 필요 없음(항상 3줄 고정이라 세로로 넘칠 일이 없음). 고정 5개뿐인 반복주기 필터는 그대로 가로 스크롤 한 줄 유지 — **폰 실기 확인 완료(2026-08-27, "응응 되네")**
  5. **"내 루틴" 화면에서 반복주기 탭을 "1회성"이 보이게 오른쪽으로 밀어둔 채로 모음집 탭으로 바꾸면 그 스크롤 위치가 그대로 유지되던 버그**: 반복주기용 `FlatList`와 모음집용 `FlatList`가 같은 JSX 자리에서 조건부로 바뀌다 보니 리액트가 같은 걸로 착각해서 네이티브 스크롤 위치까지 그대로 재사용하고 있었음 — 두 `FlatList`에 각각 다른 `key`를 줘서 탭 전환 시 완전히 새로 만들어지도록(스크롤 위치도 초기화) 수정
- **"내 루틴" 탭에서 삭제로 모음집이 텅 비면 그 모음집도 같이 삭제(2026-08-27)**: "내 루틴 탭은 보기/삭제 용도고, 모음집 이름 수정·설정은 모음집 탭에서 하니까, 내 루틴 탭에서 어떤 모음집의 루틴을 전부 지워서 0개가 되면 그 모음집도 같이 정리하자"는 사용자 아이디어 반영. `app/my-routines.tsx`에 `cleanupEmptyPresets` 추가 — 단건/선택삭제(`handleDelete`/`handleBulkDeleteSelected`) 후 방금 지운 루틴들의 `preset_id`를 모아서, 그중 살아있는 루틴이 하나도 안 남은 모음집만 골라 `deletePreset`(소프트 삭제) 호출 + 필터 목록/선택 상태 정리. 모음집 자체 소프트 삭제라 "루틴 복구" 화면에서 2주 안에 되돌릴 수 있음(단, 그 안의 루틴들은 별개로 이미 각자 소프트 삭제된 상태이므로 모음집만 복구해도 루틴은 안 돌아오고 따로 복구해야 함 — 기존 "모음집 삭제" 흐름과 동일한 정책)

**이전 세션 완료 사항 (2026-08-24)**: 신규 기능 10종 폰 QA 버그 전부 수정(드래그 라이브러리 교체, 루틴 일시정지, 모음집-루틴 연결 추적, "루틴 복구" 화면 등). **마이그레이션 `0016` 실행 확인 완료(2026-08-26), 모음집 삭제/복구 정상 동작.** 상세 배경은 `docs/study.md` 2026-08-24 섹션 참고.

### ⚠️ 확인 필요 (다음 세션 시작하면 바로 물어볼 것)

- **마이그레이션 `0023`, `0024`를 Supabase에서 실행했는지** — 안 하면 슬롯(체크형/정확한 시간) 관련 화면에서 에러남
- **알림이 슬롯 시간/모드 변경에 맞춰 재동기화되는지 폰 QA** — 슬롯을 체크형→정확한 시간으로 바꾸면 그 새 시각에 알림이 오는지 실제로 몇 분 뒤로 맞춰서 확인 (아직 미확인)
- **"오늘 목록에 추가"(스킵 되돌리기) 기능 폰 QA** — 일반 반복 루틴은 확인됨. 모음집으로 만든 루틴/1회성 루틴도 정상 동작하는지까지는 미확인
- **react-query 마이그레이션 전체 화면 최종 확인** — 오늘/캘린더/통계/설정/트래킹/모음집 재적용 버그/슬롯 커스텀 유지는 이번 세션에 폰으로 확인 완료. 즐겨찾기/영상/일기/루틴 복구 화면은 코드는 옮겼지만 이번 세션엔 별도로 집중 QA 안 함
- 날짜 메모 기능 폰으로 실기 QA (캘린더 칸에 색 바가 잘 보이는지, 추가/수정/삭제, 월간뷰·주간뷰 둘 다)
- 아침 알림에 메모가 실제로 합쳐져서 오는지 폰으로 QA (메모 있는 날 전날 밤~아침 사이 앱을 한 번 열어서 재동기화된 상태로 확인 필요)
- 영상 카테고리 추가/이름수정/삭제/복구/완전삭제 + 기본 카테고리 삭제/"기본 카테고리 생성"(추천 영상만 자동으로 다시 채워지는지) 폰으로 실기 QA
- 영상 큐레이션용 유튜브 URL을 카테고리별로 받았는지
- 카카오 로그인은 여전히 미착수(의도적으로 미룸)
- **디자인 방향 목업 나와있음 — 13단계 마무리 때 실제 앱에 반영할지 결정** (아래 "디자인 방향 목업" 섹션 참고)
- 디자인/로고 방향 정했는지, 스토어 자료(스크린샷·설명문구·앱아이콘) 준비 시작할지

### 🎨 디자인 방향 목업 (2026-08-23, 아직 실제 앱엔 미반영)

Claude 아티팩트로 만든 "타투 라인워크" 컨셉 목업 2화면: https://claude.ai/code/artifact/29866012-2df7-4d2c-a52f-225d6e4b03fb

- **컨셉**: 흰 배경 + 거의 검정(#1A1A1A) 얇은 손그림풍 라인 아이콘(타투 도안 느낌), 포인트 컬러는 보라 계열(기본 `#5B67D9`) + 민트/핑크 보조색
- **폰트 조합**: 큰 숫자·제목 = Bricolage Grotesque(두꺼운 디스플레이), 본문 = Archivo, 라벨·시간 = Space Mono(모노스페이스+자간 넓게, 도장 찍은 느낌)
- **화면1(오늘의 루틴)**: 스트릭 히어로 카드(큰 숫자+이번달 목표), 카테고리 칩, 체크리스트, 원형 + 플로팅 버튼
- **화면2(이번 주 기록)**: 도넛 차트(주간 완료율), 베스트 스트릭 카드, 카테고리별 미니 카드, 요일별 막대그래프
- 지금 실제 앱 색감·컴포넌트 스타일과는 많이 다름 — **13단계(마무리) 착수 시 이 방향으로 갈지, 어디까지 반영할지 사용자와 먼저 상의할 것**

### 📱 2026-08-23~24 신규 기능·수정사항 폰 재확인 — ✅ 전부 완료(2026-08-26)

마이그레이션 0013·0014 실행 완료 확인함. 아래는 폰 재확인 중 나온 버그와 수정 내역:

- [x] **오늘 탭 리스트/타임라인**: 전환 확인. 타임라인 같은 슬롯(아침 등)에 루틴 여러 개 몰리면 **세로로 쌓이는데, 모음집으로 만든 것만 그렇고 직접 만든 건 가로로 나뉘어 보인다는 제보** → 확인해보니 버그 아니라 "정확한 시각 vs 슬롯" 설정 차이였음(슬롯끼리는 세로로 쌓이는 게 의도된 동작, 모음집 항목 기본값이 슬롯이라 그렇게 보인 것). 순서를 바꾸려면 "내 루틴"에서 드래그해야 한다는 안내 배너를 하루 1회(또는 "다시 안 보기" 시 영구히) 노출하도록 추가
- [x] **오늘 탭 헤더**: 버튼 5개 가로 스크롤 확인함. "+ 루틴 추가"는 "오늘" 글자와 같은 줄 오른쪽 끝으로 위치 변경, 이메일 표시는 제거(설정 화면으로 이동), 상단 여백도 축소
- [x] **"내 루틴" 화면**: 필터 탭에서 "전체" 빼고 아무것도 안 뜨던 버그 발견 → 원인은 드래그 라이브러리(`react-native-draggable-flatlist`)가 Reanimated 4와 호환 안 돼서 리스트 자체가 렌더링 안 된 것 → `react-native-reorderable-list`로 교체해서 해결. 그 외 "선택 삭제"(체크박스+전체선택) 기능, "반복주기/모음집" 탭 분리, "♻️ 루틴 복구" 화면 진입 버튼 신규 추가
- [x] **캘린더 탭**: 월/주 전환 확인. 주간뷰 스크롤바가 안 보여서 스크롤 가능한지 확인 어렵다는 제보 → `showsHorizontalScrollIndicator` 켜서 명시적으로 보이게 수정
- [x] **통계 탭**: 주간/월별 전환 확인. "숨긴 항목 다시 보이기"를 여러 개 연달아 하면 스크롤 위치가 계속 튀는 문제 → "숨긴 항목" 섹션을 리스트 안(Footer)이 아니라 화면 하단 고정 영역으로 분리해서 해결. 상단 여백도 축소
- [x] **루틴 추가 화면**: 메모+사진 첨부 확인함. 카테고리 섹션은 코드상 이미 제거돼 있었고, 폰 재확인(2026-08-26) 결과 실제로도 안 남아있음 → 이전 제보는 캐시 문제였던 것으로 확인, 해결됨
- [x] **말로 루틴 추가**: 미리보기까지 갔다가 뒤로가기하면 오늘 탭까지 건너뛰던 버그(모달 프레젠테이션 때문) → 뒤로가기 시 항상 입력 화면으로 고정하도록 수정하다 무한 재귀(콜스택 초과) 크래시 발생 → 재진입 방지 플래그로 해결. 저장 완료 후에도 입력 화면으로 돌아가던 버그는 `router.dismissTo`로 오늘 탭까지 한 번에 나가도록 수정
- [x] LLM 파서 재확인 완료

**2026-08-24 추가로 나온 요청사항도 이 라운드에서 같이 처리**: 루틴 일시정지, 모음집-루틴 연결 추적, 모음집 삭제 시 루틴도 같이 삭제(안내 후), 삭제된 루틴/모음집 2주 복구 화면("루틴 복구", 선택 삭제로 즉시 완전 삭제도 가능), 로그아웃을 설정 화면으로 이동. 상세는 `docs/study.md` 2026-08-24 섹션 참고.

### 📱 실기 테스트 현황 (2026-07-22~23 폰으로 실제 테스트 시작함)

**5단계 재미 요소 (2026-07-21)** — ✅ 전부 확인 완료(2026-07-23)
- [x] **필수 형광펜**: 루틴 폼에서 "필수" 토글 켠 루틴이 오늘 탭에서 제목 뒤에 코랄색 반투명 밴드가 살짝 기울어진 채로 깔리는지 (`app/(tabs)/index.tsx`의 `requiredHighlight` 스타일)
- [x] **스트릭 이모지**: 같은 루틴을 7일 연속 체크했을 때 제목 옆에 💧 7일 형태로 뜨는지 (6일까지는 안 떠야 정상). 반복 없는(1회성) 루틴은 이모지 자체가 안 뜨는 게 맞음
- [x] **스트릭 즉시 갱신**: 체크박스를 탭한 직후 새로고침 없이 바로 이모지/일수가 바뀌는지 (`refreshStreaks` 호출 확인용)
- [x] **홈 요약바**: 리스트 하단에 "오늘 완료 N/M (%)" 문구가 뜨고, 스트릭 있는 루틴이 하나라도 있으면 "최고 기록 🔥 N일"도 같이 뜨는지
- [x] **오늘 삭제(스킵)한 날이 스트릭을 안 끊는지**: ✅ 확인 완료(2026-07-22, SQL로 테스트 루틴 만들어서 6일치 완료기록+중간에 스킵 날짜 하나 넣고 확인 — 스트릭 6일 그대로 유지됨). 공휴일 제외 토글 조합은 실제 공휴일 지나갈 때 추가로 확인 필요

**8단계 캘린더 탭 (2026-07-22)** — ✅ 전부 확인 완료(2026-07-23)
- [x] 월별 날짜 컬러 코딩이 맞는지 (🟢다 완료/🟠일부완료/🔴필수 놓침), 오늘 이후 미래 날짜는 색이 안 뜨는지 — ✅ 확인함
- [x] 날짜 클릭 시 바텀시트에 그날 루틴 체크리스트가 뜨는지 — ✅ 확인함. 단, **루틴이 많으면 스크롤이 안 되던 버그 발견 → 수정함(2026-07-22, `ScrollView` 누락이 원인)**
- [x] 바텀시트의 "📔 일기 보기" 버튼이 그 날짜의 일기 화면으로 이동하는지 — ✅ 확인함
- [x] 오늘 날짜 상세보기에서 루틴 행을 탭하면 체크/해제가 토글되는지(트래킹형도 숫자 없이 체크만), 과거 날짜는 여전히 읽기 전용인지 — ✅ 확인함(2026-07-23)

**9단계 일기 기능 (2026-07-22)** — ✅ 전부 확인 완료(2026-07-23)
- [x] 오늘 탭 헤더 "📔 일기" 버튼 → 오늘 날짜 일기 화면 진입, 저장 후 뒤로가기 잘 되는지 — ✅ 확인함
- [x] 같은 날짜 일기를 다시 열면 이전에 쓴 내용이 그대로 불러와지는지 (수정), 일기 삭제 버튼도 동작하는지 — ✅ 확인함
- [x] 과거 날짜 일기도 캘린더에서 열어 자유롭게 수정 가능한지 — ✅ 확인함
- [x] 다 적고 화면 탭해도 키보드가 안 내려가던 문제 → 제목 옆 "키보드 닫기" 버튼 추가로 수정 → ✅ 확인함. 이후 "스크롤 내리면 키보드 닫힘" 동작도 추가함(2026-07-23, `keyboardDismissMode="on-drag"`) → **폰 재확인 결과 안 닫히는 버그 발견(2026-07-26)**: 내용이 짧으면 실제 스크롤이 안 일어나 드래그 자체가 인식 안 되는 게 원인 → 안 보이는 여백을 화면 아래에 깔아 항상 스크롤 가능하게 만들고 `onScrollBeginDrag`로 드래그 시작 즉시 닫히도록 수정 → ✅ 확인 완료

**10단계 통계 탭 (2026-07-22)** — ✅ 전부 확인 완료(2026-07-23)
- [x] 최근 7일 수행률 %, 루틴별 카드(현재 스트릭/역대 최고 스트릭/전체기간 수행률)가 오늘 탭 스트릭과 값이 일치하는지: ✅ 확인 완료(2026-07-22). 처음 테스트 때 "역대 최고 스트릭"이 6일이 아니라 2일로 나와서 버그인 줄 알았는데, 원인은 테스트 루틴의 `created_at`이 SQL로 백필한 완료기록 날짜보다 최근이라 생긴 것(루틴이 존재하기 전 날짜는 통계에서 정당하게 제외됨) — `created_at`을 뒤로 당겨서 재테스트하니 정상적으로 6일로 일치함
- [x] 루틴이 하나도 없는 신규 상태에서 "아직 기록이 없어요, 루틴을 먼저 체크해보세요" 빈 상태 문구가 뜨는지 — ✅ 확인 완료(2026-07-23, 루틴 소프트 삭제 후 확인하고 되돌림)

**11단계 영상 큐레이션 (2026-07-22~23, SQL로 테스트 영상 하나 넣어서 확인)** — ✅ 전부 확인 완료(2026-07-23)
- [x] 오늘 탭 헤더 "🎬 영상" 버튼 → 카테고리 가로 탭 + 2열 그리드가 뜨는지 — ✅ 확인함
- [x] 루틴 폼의 "카테고리" 칩 선택 + "영상 선택하기" → 바텀시트에서 영상 고르면 폼에 썸네일+제목이 반영되는지 — ✅ 확인함
- [x] 영상 연결된 루틴이 오늘 탭 리스트에서 ▶ 재생 아이콘으로 표시되고 탭하면 그 영상 재생 화면으로 이동하는지 — ✅ 확인함
- [x] **영상 재생 화면에서 유튜브가 실제로 재생되는지** — 여러 번 문제 있었음: 헤더/제스처 문제(고침) → 채널 방문 버튼이 화면 하단 제스처 영역과 겹쳐 안 눌림(여백 늘려서 고침) → 영상 자체가 계속 검은 화면(WebView 레이어/뷰포트 이슈로 추정해 여러 번 시도했지만 해결 안 됨) → 결국 `react-native-webview` 직접 제어를 버리고 `react-native-youtube-iframe` 라이브러리로 교체(2026-07-23) → **✅ 재생 확인 완료**
- [x] 채널명/채널 방문 버튼 동작하는지 — ✅ 확인함(테스트 데이터의 `channel_url`이 플레이스홀더라 일반 유튜브 홈으로 감, 정상 동작)

**12단계 알림 + 개인 영상 추가 기능 (2026-07-26)** — ✅ 전부 확인 완료(2026-07-26)
- [x] 설정 화면(⚙️) 진입, 슬롯 알림 토글 켜기 — ✅ 확인함
- [x] "자기전" 슬롯 알림 켠 상태에서 필수 루틴 미완료 시 리마인더 예약되는지 — ✅ 확인함("자기전 알림 되는데"로 확인)
- [x] 개인 영상 추가(유튜브 링크 붙여넣기 → oEmbed로 제목/썸네일 자동 채워짐) — ✅ 확인함
- [x] "채널 방문" 버튼이 정확히 그 채널로 이동하는지 — 처음엔 oEmbed의 `@핸들` 주소를 안드로이드 유튜브 앱이 못 받아서 홈으로 떨어지는 버그 발견 → 영상 페이지 HTML에서 표준 채널ID(`UC...`) 직접 추출하는 방식으로 수정 → ✅ 확인함
- [x] 루틴에 연결된 영상 삭제 — 처음엔 DB 제약으로 삭제 자체가 막혀있었음 → FK를 `ON DELETE SET NULL`로 바꿔서 "연결된 루틴 있어도 삭제 가능, 대신 그 루틴의 영상 연결만 해제" 방식으로 변경 → ✅ 확인함
- [x] "내 영상" / "추천 영상" 탭 분리, 추천 영상에서 "내 그리드에 추가" 동작 — 코드 완료, 폰 재확인은 다음 세션에 (오늘 세션 마지막에 만든 부분이라 아직 실기 테스트 전)

### ✅ 완료된 것

- 로드맵 1~4단계 전부 완료 (환경 세팅 → DB 8개 테이블+RLS → 3탭 네비게이션 → 구글 로그인 → 오늘 탭 → 루틴 추가/수정 폼). 마일스톤1("루틴 만들고 체크하는 최소 기능") 달성
- **오늘 탭**: `app/(tabs)/index.tsx` — 시각순/슬롯순 정렬, 체크/트래킹 완료, 지금 시간대 테두리 강조, 공휴일 배너, 스와이프로 "오늘만 삭제"
- **루틴 추가/수정 폼**: `app/routine-form.tsx` — 시각(범위) 또는 슬롯, 반복, 필수, 영상 연결(11단계에서 추가됨), 메모+사진 첨부(2026-08-23 추가), 공휴일 제외, 즐겨찾기 저장 토글, "루틴삭제"(확인창 후 반복 전체 완전 삭제). 개별 루틴 알림 필드는 12단계에서 "너무 시끄러울 것 같다"는 이유로 최종적으로 안 넣기로 확정(2026-07-26) — 슬롯 알림 + 자기전 리마인더로 대체. **카테고리 선택 UI는 2026-08-23에 제거함**(영상 선택 화면에서도 안 쓰이던 죽은 기능이었음이 확인됨)
- **루틴 모음집**: `app/presets.tsx`, `app/preset-form.tsx`, `lib/presets.ts` — 반복 규칙·공휴일 제외는 묶음 전체 설정, 항목은 개별 입력(완료 시 접힘), 즐겨찾기에서 항목 불러오기 가능
- **루틴 즐겨찾기**: `app/favorites.tsx`, `app/favorite-form.tsx`, `lib/favorites.ts`, `components/FavoritePicker.tsx` — 반복 규칙 없는 개별 템플릿. 루틴 폼에서 "바로 추가"/"수정해서 추가" 선택 가능
- **삭제 정책**: 오늘 탭 스와이프 = 그날만 빠짐(반복 유지, `routine_skip_dates`), 수정 화면 "루틴삭제" = 반복 전체 완전 삭제(`routines.deleted_at`). 완료 기록(`routine_completions`)은 어느 쪽이든 항상 보존
- **재미 요소 (로드맵 5단계, 코드 완료 — 폰 실기 테스트는 위 체크리스트 참고)**: `lib/routines.ts`에 `fetchStreaks`/`emojiForStreak`(스트릭 계산, `streak_emoji_configs` 테이블 기반, 오늘삭제·공휴일은 쉬는 날로 취급해 스트릭 안 끊김), `app/(tabs)/index.tsx`에 필수 형광펜 밴드 + 루틴별 스트릭 이모지 뱃지 + 체크 즉시 스트릭 갱신 + 하단 완료율/최고기록 요약바
- **정규식 파싱 (로드맵 6단계, 완료)**: `lib/parse-routine-input.ts` — 자연어 문장에서 반복/시간/필수/트래킹 단위를 정규식으로 추출하는 순수 함수(`parseRoutineInput`). project-spec.md 3번 표의 예시 13개로 검증 완료. 아직 이 함수를 부르는 입력 화면(배너/바텀시트, 4-8)은 없음 — 7단계(LLM 연동)에서 미리보기→확정 흐름과 같이 만들 예정. `needsLlmFallback` 플래그는 7단계에서 실제 LLM 호출 분기에 씀
- **LLM 무료 호출 한도 정책 확정 (2026-07-21, 문서만 — 아직 미구현)**: "계정당 총 10회" → 가입 순번 기반 평생 한도로 변경. 최초 1~10,000번째 가입자는 10회, 10,001번째부터는 5회(월 초기화 없음). 소진 시 "요금제 안내"(출시 전이라 "출시 예정" 문구) 노출. `project-spec.md` 3번·4-13번 참고, 실제 DB 반영(가입 순번 컬럼 등)은 7단계 착수 시
- **잠금화면/홈 위젯 아이디어는 보류, 문서에만 메모**: `project-spec.md` 8-3 참고 — 안드로이드는 네이티브(Kotlin) 코드 필요, iOS 잠금화면 위젯은 Mac+Swift 필수라 지금 Windows 환경으론 개발 불가. 로드맵엔 미포함
- **캘린더 탭 (로드맵 8단계, 완료, 2026-07-22)**: `app/(tabs)/calendar.tsx` — `react-native-calendars`로 월뷰 구현. `lib/routines.ts`의 `fetchMonthData`/`routinesForDate`/`computeDayStatus`가 월 단위로 루틴/완료기록/오늘삭제/공휴일을 한 번에 조회해서 날짜별 🟢다완료/🟠일부완료/🔴필수놓침 판정(오늘 이후 미래 날짜는 판정 안 함). 날짜 클릭 시 바텀시트로 그날 루틴 체크리스트 + "📔 일기 보기" 버튼. **폰 실기 테스트(2026-07-22) 후 수정**: 목록 스크롤 안 되던 버그 고침(`ScrollView` 추가), 오늘 날짜는 읽기 전용에서 탭-토글(체크/해제) 가능하도록 변경 — 과거 날짜만 읽기 전용 유지(기획대로), 트래킹형도 이 화면에선 숫자 없이 체크만
- **일기 기능 (로드맵 9단계, 완료, 2026-07-22)**: `lib/diary.ts`(fetchDiary/saveDiary/deleteDiary), `app/diary-form.tsx` — 날짜 하나당 일기 하나, 생성/수정/삭제. 오늘 탭 헤더 "📔 일기" 버튼(오늘 날짜로 진입)과 캘린더 날짜 상세의 "📔 일기 보기" 버튼(그 날짜로 진입) 두 군데서 접근. 과거 날짜도 자유롭게 소급 수정 가능(기획대로). **폰 실기 테스트(2026-07-22) 후 수정**: 일기장이 화면 대부분을 차지해서 탭-바깥-닫기가 안 먹던 문제 → 제목 옆 "키보드 닫기" 버튼 추가
- **통계 탭 (로드맵 10단계, 완료, 2026-07-22)**: `app/(tabs)/stats.tsx`, `lib/routines.ts`의 `fetchStats`(+`computeLifetimeStats`) — 최근 7일 전체 수행률 카드 + 루틴별 카드(현재 스트릭/역대 최고 스트릭/전체기간 수행률 N/M). 기획서에 "수행률/스트릭 기본"만 명시돼 있어서 상세 버전(전체기간 수행률+역대 최고 스트릭 포함)으로 할지 심플 버전으로 할지 사용자에게 확인 후 상세 버전으로 결정함. 루틴이 하나도 없으면 빈 상태 문구
- **영상 큐레이션 (로드맵 11단계, 코드 완료 2026-07-22, 콘텐츠는 대기)**: `lib/videos.ts`(fetchCategories/fetchVideosByCategory/fetchVideoById/extractYoutubeId), `components/CategoryVideoGrid.tsx`(카테고리 가로 탭+2열 그리드, 공용 컴포넌트), `app/videos.tsx`(오늘 탭 헤더 "🎬 영상" 버튼으로 독립 진입), `app/video-player.tsx`(유튜브 재생 + 채널명/링크), `components/VideoPicker.tsx`(루틴 폼의 "영상 연결" 바텀시트, `FavoritePicker`와 동일한 모달 패턴). `app/routine-form.tsx`에 "카테고리" 칩 선택 + "영상 연결" 필드 추가(`routines.category_id`/`video_id`는 0001 마이그레이션 때부터 이미 있던 컬럼, UI만 새로 붙임). 오늘 탭 리스트에서 영상 연결된 루틴은 ▶ 아이콘으로 재생 화면 바로 이동. `videos` 테이블이 비어있어서 실제 콘텐츠는 대기 중(사용자가 카테고리별 유튜브 URL 목록을 주면 oEmbed로 채워 넣기로 함, `memory/video_curation_plan.md` 참고). **영상 재생 라이브러리 변경(2026-07-23)**: 처음엔 `react-native-webview`로 직접 구현했는데 안드로이드에서 검은 화면 버그가 계속 재현되어(레이어 타입, 뷰포트 스케일링 등 여러 시도 실패) 결국 `react-native-youtube-iframe`(webview 기반이지만 이런 이슈들을 이미 처리해둔 전용 라이브러리)로 교체함 — 폰 재확인 대기 중
- **로컬 알림 (로드맵 12단계, 완료, 2026-07-26)**: `lib/notifications.ts`(`syncSlotAlarms`/`syncReminderAlarm`), `app/settings.tsx`(신규, 슬롯 4개 시간/알림 토글, 3개 탭 공통 헤더 ⚙️ 아이콘으로 진입). 슬롯 알림은 매일 반복 예약(`SchedulableTriggerInputTypes.DAILY`), 리마인더는 "앱 열 때마다(또는 완료 체크할 때마다) 다음 발송 시점을 다시 계산해서 1회성으로 재예약"하는 방식(`DATE` 트리거) — 서버 없이 로컬 알림만으로 "오늘 필수 루틴 미완료 시에만" 조건부 발송을 구현하기 위한 절충안. 개별 루틴 알림(4-9)은 이번엔 제외
- **사용자 개인 영상 추가 (로드맵 4-10-1, 완료, 2026-07-26)**: `lib/videos.ts`(`createUserVideo`/`deleteUserVideo`/`countRoutinesUsingVideo`), `videos.user_id` 컬럼 추가(0010 마이그레이션, 새 테이블 없이 기존 구조 재사용). 유튜브 링크 붙여넣으면 oEmbed로 메타데이터 자동 채움, 본인에게만 비공개로 보임, 삭제 가능(루틴에 연결돼 있어도 삭제 가능하도록 FK를 `ON DELETE SET NULL`로 변경, 0011 마이그레이션). **채널 링크 버그 수정**: oEmbed가 주는 `@핸들` 형식 채널 주소를 안드로이드 유튜브 앱이 못 받아 홈으로 떨어지는 문제 발견 → 영상 페이지 HTML에서 표준 채널ID(`UC...`)를 직접 추출해 사용하도록 변경
- **영상 구조 변경 (2026-07-26, 4-10 참고)**: 관리자 큐레이션 공용 영상을 모든 유저 그리드에 자동 노출하던 방식 → "추천 영상" 탭(`components/RecommendedVideoGrid.tsx`, 신규)에서 골라 "내 그리드에 추가"해야만 보이는 방식으로 변경. `app/videos.tsx`에 "내 영상"/"추천 영상" 탭 토글 추가. 루틴 폼의 영상 연결은 기존처럼 "내 영상" 그리드만 사용. 이 구조 변경 이후 실기 테스트는 아직 안 함(다음 세션에)
- **LLM 파서 실전 QA + 보강 (2026-08-23)**: `lib/parse-routine-input.ts`(정규식) — 반복(항상=매일), 시간대(밤/정오/자정/해질녘), 트래킹(몇회/개수/횟수 등 숫자 없어도 인식), 제목 정제(명령형 꼬리표·의도 표현·확인/체크 어미·중복 키워드 전부 제거)를 실제 문장 20개+로 테스트하며 계속 보강함. `supabase/functions/parse-routine/index.ts`(AI 프롬프트)도 같은 기준으로 시간대·트래킹 규칙 보강 + "애매해도 절대 JSON 벗어나지 말고 최대한 추출하라" 규칙 추가. `app/llm-input.tsx`에 "AI로 정확하게 분석"(강제 AI 호출) 버튼 신규 추가, 뒤로가기 시 입력 유지(단 실제 저장 완료 시엔 초기화)
- **UX 점검 신규 기능 10종 (2026-08-23, 로드맵 외)**: 다른 루틴 앱과 비교해서 빠진 것 점검 후 구현. ① 오늘 탭 타임라인 뷰(`app/(tabs)/index.tsx`의 `TimelineView` — 슬롯형은 시작 시각에 작은 블록만, 겹치는 시간대는 자동으로 옆으로 배치) ② 캘린더 주간 뷰(가로 요일 컬럼, `lib/routines.ts`의 `fetchWeekData`) ③ 통계 주간/월별 전환 + 카드 숨기기(`routines.hide_from_stats`, 마이그레이션 `0013`) ④ "내 루틴" 전체보기 화면 신규(`app/my-routines.tsx` — 필터+`react-native-draggable-flatlist` 드래그 정렬+삭제, `routines.sort_order` 재사용) ⑤ 루틴 메모+사진 첨부(마이그레이션 `0014`, `routines.memo`/`photo_url` 컬럼 + Storage 버킷 `routine-photos` 신규, `expo-image-picker`/`expo-file-system` 추가). 마이그레이션 0013·0014 실행 완료 확인됨
- **폰 실기 QA 2라운드 + 신규 기능 (2026-08-24)**: 위 10종 기능을 폰으로 테스트하며 나온 버그를 전부 고침(상세는 위 "📱 2026-08-23~24" 체크리스트 참고). 그 김에 로드맵 외로 추가한 것: ① **드래그 정렬 라이브러리 교체** — `react-native-draggable-flatlist`(Reanimated 4 비호환으로 "내 루틴" 화면 자체가 안 뜨던 원인)를 `react-native-reorderable-list`로 교체(`app/my-routines.tsx`) ② **루틴 일시정지**(`routines.is_paused`, 마이그레이션 `0015`) — 삭제 아니고 오늘탭/캘린더/통계에서만 숨김, `matchesToday()`에서 일괄 처리 ③ **모음집-루틴 연결 추적**(`routines.preset_id`, 마이그레이션 `0015`) + "내 루틴"에 반복주기/모음집 탭 분리, 모음집 화면에 전체 비활성화/활성화 일괄 액션 ④ **모음집도 소프트 삭제로 전환**(`routine_presets.deleted_at`, 마이그레이션 `0016`) — 하드 삭제 시 FK(`on delete set null`)로 연결이 끊겨서 "모음집째로 복구"가 불가능했던 문제 해결 ⑤ **"루틴 복구" 화면 신규**(`app/routine-trash.tsx`) — 삭제된 루틴/모음집을 2주간 보관, 모음집으로 삭제된 건 묶어서 "모음집 복구" 한 번에, "선택 삭제"로 2주 안 기다리고 즉시 완전 삭제도 가능. 앱 열 때 하루 1회 조용히 2주 지난 것들 자동 정리(`lib/routines.ts`의 `purgeOldDeletedRoutines`/`lib/presets.ts`의 `purgeOldDeletedPresets`) ⑥ 로그아웃을 오늘 탭에서 설정 화면으로 이동, 오늘/통계 탭 상단 여백 축소
- **GitHub 연결**: 비공개 저장소 `github.com/leeyonghee030/daily-loop`, 로컬 브랜치 `master`→`main`
- **"작업끝" 커맨드**: `.claude/commands/작업끝.md` — 공부정리+문서 최신화+커밋+푸시를 한 번에 실행 (아래 트리거 섹션 참고)
- 카카오 로그인은 아직 안 함 (Firebase Custom Token 절차 필요, 의도적으로 미룸)

### 알아두면 좋은 것

- 개발 서버는 `npx expo start`로 실행, 포트 충돌 나면 `--port` 옵션으로 다른 포트 사용
- **구글 로그인 테스트 시 주의**: Expo Go의 `exp://192.168.x.x:PORT/...` 주소는 호스트가 IP 형태라 Supabase Auth 서버 버그로 리다이렉트가 항상 막힘 (`supabase/auth#2039`, 와일드카드 등록해도 소용없음). 로그인 테스트할 때는 `npx expo start --tunnel`로 실행하고, Supabase 대시보드 Redirect URLs에 `exp://*.exp.direct/**` 등록해서 우회. 평소 개발은 그냥 `npx expo start`(LAN 모드)면 충분
- **`--tunnel` 최초 실행 시 "ngrok tunnel took too long to connect" 에러 날 수 있음(2026-07-26 겪음)**: `npm install --save-dev @expo/ngrok` 설치 후에도 계속 나면, ngrok이 무료 계정 인증(authtoken)을 요구하는 정책으로 바뀐 것 — https://dashboard.ngrok.com/signup 가입 → authtoken 발급 → `npx ngrok authtoken <토큰>`으로 등록하면 해결됨. 번들된 ngrok이 구버전(2.x)이라 `authtoken` 서브커맨드를 바로 씀(`ngrok config add-authtoken`은 최신 v3 문법이라 안 먹힘)
- **`--tunnel`에서 "CommandError: failed to start tunnel / remote gone away" 에러(2026-08-24 겪음)**: authtoken은 등록돼 있는데도 발생 — 원인은 이 네트워크(또는 PC 백신의 HTTPS 검사 기능)가 `tunnel.ngrok.com`으로 가는 TLS 연결을 막고 있는 것이었음(TCP 연결은 되는데 TLS 핸드셰이크에서 실패). ngrok 자체 장애는 아님(https://status.ngrok.com 정상 확인). **휴대폰 핫스팟으로 바꾸면 바로 해결됨** — 평소엔 와이파이 쓰고 로그인 테스트할 때만 핫스팟 사용 권장
- **같은 에러가 2026-08-26엔 핫스팟으로도 안 고쳐진 적 있음**: 이땐 원인이 달랐음 — `@expo/ngrok`이 번들한 게 ngrok **v2**(2.3.41)인데, ngrok 서버 쪽에서 v2 프로토콜 지원을 줄이면서 이 계정은 최소 v3.20.0 이상을 요구해서 접속 자체가 거부됨. 우회하려고 `winget install ngrok.ngrok` → `ngrok update`로 최신 v3(3.39.11)를 받아서 수동으로 `ngrok http <포트>` + Expo Go에 URL 직접 입력하는 방법을 시도했으나, **Windows Defender가 새로 받은 ngrok.exe를 다운로드되자마자 계속 검역(삭제)** — 폴더 단위 예외 등록도, "변조 방지"(Tamper Protection) 끄기도 둘 다 안 먹혀서 결국 포기함(회사/학교 관리 정책이나 클라우드 평판 검사 같은 더 깊은 제약일 가능성). 변조 방지는 다시 켜두기로 함
- **2026-08-31, ngrok을 완전히 버리고 USB+`adb reverse`로 전환해서 해결함**: `winget install --id Google.PlatformTools`로 adb 설치(Google 서명 정식 배포판이라 Defender에 안 걸림). 순서: 폰 개발자 옵션에서 USB 디버깅 켜기(빌드번호 연속 탭으로 개발자 모드 먼저 활성화 필요할 수 있음) → USB 연결 시 뜨는 허용 팝업 확인(폰이 충전 전용 모드면 팝업 자체가 안 뜨므로 "파일 전송" 모드로 바꿔야 함, 케이블이 충전 전용이면 그마저도 안 됨) → `adb devices`로 잡히는지 확인 → `npx expo start`(터널 없이) → `adb reverse tcp:포트 tcp:포트` → Expo Go에서 "Enter URL manually"로 `exp://localhost:포트` 접속. `localhost` 주소라 IP 기반 리다이렉트 버그 자체를 안 타서 정상 로그인됨. adb 설치 경로: `%LOCALAPPDATA%\Microsoft\WinGet\Packages\Google.PlatformTools_...\platform-tools\adb.exe`(새 터미널 열면 PATH에 잡힘). **평소 개발엔 USB 필요 없음, 로그아웃 후 재로그인 테스트할 때만 이 방법 사용**
- `.expo/types/router.d.ts`가 가끔 깨진 상태로 캐싱돼서 `npx tsc`가 이상한 문법 에러를 뱉을 때가 있음(2026-08-27 원인 특정: 실행 중인 expo 서버가 이 파일을 다시 쓸 때 기존 내용을 자르지 않고 덮어써서, 새 내용이 이전보다 짧으면 뒤에 예전 내용 찌꺼기가 그대로 남아 문법 깨짐 — 파일 끝을 보면 `}`로 정상 종료된 블록 뒤에 또 텍스트가 붙어있는 형태). 가장 빠른 해결은 파일 열어서 **맨 처음 `}}}` 로 끝나는 부분 이후의 찌꺼기 줄만 지우기**. 안 되면 `rm -rf .expo/types` 후 `npx expo start` 한 번 돌리면 재생성됨
- `.env`에 Supabase URL/anon key 있음 (git에는 없음, 로컬에만 존재 — 새 기기에서 작업 시 다시 채워야 함)
- 공휴일 데이터(`holidays` 테이블)는 2026~2027년치만 시드해둠 — 2028년 되기 전에 갱신 필요
- 즐겨찾기 "바로 추가"를 테스트하면서 중복 루틴이 쌓이면 `supabase/cleanup_duplicate_routines.sql` 참고

## 문서 구조 (`docs/`)

작업 전에 아래 순서로 참고할 것 — 뒤로 갈수록 구체적:

1. `docs/project-spec.md` — 서비스 기획 확정 문서 (기술스택, 화면 구성, 컬러, 수익모델, 로드맵 등 전체 기획). **새 대화 시작 시 이 문서를 먼저 읽고 이어갈 것**
2. `docs/user-flow.md` — 화면 간 사용자 이동 흐름
3. `docs/domain-model.md` — 엔티티/관계/비즈니스 규칙
4. `docs/erd.md` — 실제 DB 테이블 설계 (Supabase/PostgreSQL)
5. `docs/study.md` — 학습 기록 (아래 참고)

기획 관련 새로운 결정이 나오면 해당 문서를 바로 업데이트하고, 문서 내 "아직 정해지지 않은 것" 체크리스트도 같이 갱신할 것.

## 학습 기록 — "공부정리" 트리거

사용자가 **"공부정리"**라고 말하면:
- `docs/study.md`를 읽고, 그 세션에서 한 작업을 새 날짜 섹션으로 이어붙여 정리 (기존 기록은 절대 덮어쓰지 않고 누적)
- 형식: 오늘 한 일 요약 → 새로 나온 개념 쉬운 설명(표) → 확정한 결정 목록 → 왜 그렇게 했는지 이유
- 대상 독자는 비개발자 학습자 — 전문 용어는 항상 쉬운 말로 풀어서 설명

## 작업 마무리 — "작업끝" 트리거

사용자가 **"작업끝"**이라고 말하면(또는 `/작업끝` 실행하면) `.claude/commands/작업끝.md`에 정의된 순서대로 진행할 것:
1. 공부정리 (위 트리거와 동일 로직)
2. `docs/project-spec.md` 로드맵 상태 + 이 파일의 "현재 진행 상황" 최신화 (다음 세션이 바로 이어갈 수 있게 이어갈 지점 한 줄 명시)
3. Git 커밋 (다시 승인 안 물어봄 — "작업끝" 자체가 요청)
4. Git 푸시 (origin/main, 마찬가지로 재승인 없이 진행)
5. 마무리 보고

터미널을 끄고 다른 데서 새로 세션을 열어도, 이 문서(CLAUDE.md)를 읽으면 이 트리거를 그대로 인식하고 실행할 수 있음.

## 작업 스타일

- 응답은 한국어로
- 애매한 설계 결정은 추측해서 진행하지 말고 먼저 질문할 것 (특히 데이터 구조에 영향을 주는 것)
- 개발은 `project-spec.md` 8-1 로드맵 순서로 진행 중 — 새 단계 들어갈 때마다 로드맵 표의 "상태" 컬럼과 위 "현재 진행 상황"을 갱신할 것
- 코드 변경 후에는 `npx tsc --noEmit`으로 타입체크, 가능하면 `npx expo start`로 서버가 에러 없이 뜨는지 확인 후 커밋
